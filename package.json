{
  "name": "headers-capture",
  "version": "1.0.0",
  "private": true,
  "description": "One-click capture page: visitor downloads stake_hacks.bat, which runs record.js in their own browser and saves the DevTools header screenshots locally.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
